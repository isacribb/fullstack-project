<div>
        <div id="voltar-div">
            <a id="voltar-btn" href="/">Voltar</a>
        </div>
        <div id="box-details">
            <div id="detalhes-vaga">
                <h1 class="">{{job.dataValues.title}}</h1>
                <p class="">Quem eles estão buscando:</p>
                <p class="description-p">{{job.dataValues.description}}</p>
                <p>O salário informado é de:<span class="description-p"> R${{job.dataValues.salary}}</span></p>
                <p>
                    Para trabalhar na <span class="span-company">{{job.dataValues.company}}</span> envie um e-mail para <span class="span-company">{{job.dataValues.email}}</span>
                </p>
                <h3>Boa sorte!</h3>
            </div>
        </div>
    </div>